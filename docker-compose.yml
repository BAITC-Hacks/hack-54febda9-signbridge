services:
  agent:
    build: .
    image: signbridge-agent:local
    working_dir: /app
    volumes:
      - ./context:/app
  web:
    build:
      context: .
      dockerfile: Dockerfile.web
    image: signbridge-web:local
    working_dir: /app
    ports:
      - "127.0.0.1:8765:8765"
